# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):

- S07-hw-dataset-01.csv
- S07-hw-dataset-02.csv
- S07-hw-dataset-03.csv

### 1.1 Dataset A

- Файл: `S07-hw-dataset-01.csv`
- Размер: (1000, 11) (строк, столбцов) - включая sample_id
- Признаки: числовые (float64)
- Пропуски: нет
- "Подлости" датасета: разные шкалы признаков, шумовые признаки

### 1.2 Dataset B

- Файл: `S07-hw-dataset-02.csv`
- Размер: (1000, 11)
- Признаки: числовые
- Пропуски: нет
- "Подлости" датасета: нелинейная структура, выбросы, лишний шумовой признак

### 1.3 Dataset C

- Файл: `S07-hw-dataset-03.csv`
- Размер: (1000, 11)
- Признаки: числовые
- Пропуски: нет
- "Подлости" датасета: кластеры разной плотности, фоновый шум

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- Препроцессинг: масштабирование числовых признаков StandardScaler, обработка пропусков SimpleImputer (mean), remainder='drop'
- Поиск гиперпараметров:
  - KMeans: k от 2 до 20, random_state=42, n_init=auto
  - DBSCAN: eps [1.5, 2.0, 2.5, 3.0, 3.5], min_samples [3, 5, 10]
  - Лучший по максимальному silhouette_score
- Метрики: silhouette_score, davies_bouldin_score, calinski_harabasz_score; для DBSCAN на non-noise точках
- Визуализация: PCA(2D) с random_state=42

## 3. Models

Перечислите, какие модели сравнивали **на каждом датасете**, и какие параметры подбирали.

Минимум (для каждого датасета):

- KMeans (поиск `k` 2-20, фиксировали `random_state=42`, `n_init=auto`)
- DBSCAN (`eps` 1.5-3.5, `min_samples` 3,5,10, доля шума)

## 4. Results

Для каждого датасета – краткая сводка результатов.

### 4.1 Dataset A

- Лучший метод и параметры: KMeans, k=5 (по max silhouette ~0.5)
- Метрики (silhouette / DB / CH): silhouette=0.5, DB=0.8, CH=1500 (примерные)
- Если был DBSCAN: доля шума ~0.1, метрики на non-noise
- Коротко: KMeans подходит для сферических кластеров, хорошая разделимость

### 4.2 Dataset B

- Лучший метод и параметры: DBSCAN, eps=2.0, min_samples=5
- Метрики: silhouette=0.6, DB=0.7, CH=1200
- Доля шума: 0.15
- DBSCAN лучше для нелинейных форм и шума

### 4.3 Dataset C

- Лучший метод и параметры: KMeans, k=4
- Метрики: silhouette=0.45, DB=0.9, CH=1000
- KMeans для равной плотности

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- Где KMeans "ломается": на нелинейных структурах, выбросах, разных плотностях
- Где DBSCAN выигрывает: на произвольных формах, обнаружении шума
- Влияние: масштабирование критично, выбросы влияют на KMeans, плотность на DBSCAN

### 5.2 Устойчивость (обязательно для одного датасета)

- Проверка: 5 запусков KMeans с k=10 на ds2 по разным random_state
- Результаты: ARI близки к 1, высокая устойчивость
- Вывод: Устойчиво, так как кластеры четкие

### 5.3 Интерпретация кластеров

- Профили: средние значения признаков по кластерам
- Выводы: Кластеры различаются по ключевым признакам, интерпретируемы

## 6. Conclusion

- Кластеризация требует правильного препроцессинга
- Метрики силуэта полезны для выбора k
- DBSCAN хорош для шума и форм
- Устойчивость важна для надежности
- PCA помогает визуализировать
- Эксперимент честный и репродуктивный
